import { JsonDestringifyPipe } from './json-destringify.pipe';

describe('JsonDestringifyPipe', () => {
  it('create an instance', () => {
    const pipe = new JsonDestringifyPipe();
    expect(pipe).toBeTruthy();
  });
});
